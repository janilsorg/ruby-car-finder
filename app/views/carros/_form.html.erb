

<%= form_with(model: carro, local: true) do |form| %>
  <% if carro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carro.errors.count, "error") %> prohibited this carro from being saved:</h2>

      <ul>
        <% carro.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :ano %>
    <%= form.text_field :ano %>
  </div>


  <div class="field">
    <%= form.label :marca_id %>
    <%= form.text_field :marca_id %>
  </div>


<!--   
  <div>
  <%= form.select(:marca_id) do %>
    <% @marcas.each do |c| %>
      <%= content_tag(:option, c.nome, value: c.id) %>
    <% end %>
  <% end %>
  </div>
-->


  <div class="actions">
    <%= form.submit "Create Carro", :class => "btn btn-primary" %>
  </div>


<% end %>
